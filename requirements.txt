# ── Core ─────────────────────────────────────────────────────
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7
pandas>=2.0

# ── MLflow — experiment tracking & model registry ────────────
mlflow>=2.10

# ── FastAPI — inference API ──────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# ── DVC — data & pipeline versioning ─────────────────────────
dvc>=3.0

# ── Dev / Test ───────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
flake8>=7.0
black>=24.0
isort>=5.13

# ── Data preprocessing / feature engineering ─────────────────
scikit-learn>=1.4

# ── Drift monitoring (optional EvidentlyAI integration) ───────
evidently>=0.4
